`min + (int)(Math.random() * (max - min + 1))`
min is the bottom of the range and max is the top
- example: to get a random number between 10 and 40
	- `int randomNum = 10 + (int)(Math.random() * 31);`